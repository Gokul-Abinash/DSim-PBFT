var _ = require("graphlib-dot/lib/lodash");
var grammar = require("graphlib-dot/lib/dot-grammar");
var buildGraph = require("graphlib-dot/lib/build-graph");

module.exports = function readMany(str) {
  var parseTree = grammar.parse(str);
  return _.map(parseTree, buildGraph);
};
